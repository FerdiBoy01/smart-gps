# PRATIA Frontend

This is a Vite + React frontend scaffold with Tailwind CSS pinned to 3.4.17.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Tailwind is configured in `tailwind.config.cjs` and the CSS entry is `src/index.css`.
